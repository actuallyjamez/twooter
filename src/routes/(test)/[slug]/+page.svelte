<script lang="ts">
	// import type { PageData } from "../$types";

	// export let data: PageData;
</script>

this is a user page for a user named "user" and the data is passed in as a prop. the data is fetched
in the route handler and passed in as a prop to the page component.
